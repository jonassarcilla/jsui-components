import { ButtonProps } from './Button';

const primary: ButtonProps = {
  children: 'This is a long button',
  id: 'btnPrimary',
};

const mockButtonProps = {
  primary,
};

export default mockButtonProps;
